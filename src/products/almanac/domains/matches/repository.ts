import type { CreditedScorerTotalRecord } from './types';

// Persistence is disabled until the replacement schemas are defined.
export const listCreditedScorerTotalRecords = async (): Promise<
  CreditedScorerTotalRecord[]
> => {
  return [];
};
